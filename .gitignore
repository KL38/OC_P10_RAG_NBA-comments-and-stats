# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv


# Private folder
/brief
/.claude

# Datasets (raw API dumps + processed parquet)
/data/

# FAISS vector store (build artifact, regenerable via scripts/build_index.py)
# Exception: the PDF-only index used by the Streamlit app is versioned for deployment
/vector_db/*
!/vector_db/faiss_index_pdf.idx
!/vector_db/document_chunks_pdf.jsonl

# Local SQLite fallback DB (regenerable via scripts/load_excel_to_db.py)
/db/

# Environment variables (API keys etc.)
.env

Livrables 
/livrables/

